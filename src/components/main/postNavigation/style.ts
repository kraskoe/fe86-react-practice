import styled from 'styled-components';

export const PostNavWrapper = styled.div`
	display: flex;
	flex-direction: column;
`

export const PostNavTitle = styled.div`
	color: ${props => props.theme.textPale};
`